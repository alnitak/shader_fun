name: shader_fun
description: "A new Flutter package project."
version: 0.1.0
publish_to: 'none'
homepage:

environment:
  sdk: ^3.13.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_recorder: ^2.0.4
    # path: ../flutter_recorder
  flutter_scene: ^0.23.0
    # path: ../../tmp/flutter_scene/packages/flutter_scene
  flutter_soloud: ^5.1.1
    # path: ../flutter_soloud
  flat_buffers: ^25.9.23
  image: 4.10.1
  listen: ^1.0.1
  vector_math: ^2.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  hooks: ^2.2.0

dependency_overrides:
  code_assets: ^2.0.0

flutter:
  assets:
    - flutter_scene_generated/
